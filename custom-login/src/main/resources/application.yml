server:
  port: 9090

spring:
  security:
    oauth2:
      client:
        provider:
          okta:
            authorization-uri: http://localhost:${server.port}/custom-login
            user-name-attribute: email

okta:
  oauth2:
    redirect-uri: /authorization-code/callback
    scopes:
    - openid
    - email
    - profile
    client-id: 0oaim3svp0qe94E094x6
    client-secret: CB4XB96VW1fe_47JZVLDkNoImymXhVZucQgooITV
    issuer: https://dev-310801.okta.com/oauth2/default